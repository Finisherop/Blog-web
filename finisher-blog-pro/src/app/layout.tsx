import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Finisher Blog Pro - Professional Blog CMS",
  description: "A modern, professional blog CMS built with Next.js, Firebase, and Tailwind CSS",
  keywords: "blog, cms, nextjs, firebase, tailwind",
  authors: [{ name: "Finisher" }],
  openGraph: {
    title: "Finisher Blog Pro",
    description: "Professional Blog CMS",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} antialiased`}>
        <div className="min-h-screen flex flex-col">
          <Navbar />
          <main className="flex-grow pt-16">
            {children}
          </main>
          <Footer />
        </div>
      </body>
    </html>
  );
}
